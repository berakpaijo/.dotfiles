-- Conky, a system monitor https://github.com/brndnmtthws/conky
--
-- This configuration file is Lua code. You can write code in here, and it will
-- execute when Conky loads. You can use it to generate your own advanced
-- configurations.
--
-- Try this (remove the `--`):
--
--   print("Loading Conky config")
--
-- For more on Lua, see:
-- https://www.lua.org/pil/contents.html

conky.config = {
    alignment = 'bottom_left',
    background = true,
    border_width = 1,
    cpu_avg_samples = 2,
    default_color = 'white',
    default_outline_color = 'white',
    default_shade_color = 'white',
    double_buffer = true,
    draw_borders = true,
    draw_graph_borders = true,
    draw_outline = false,
    draw_shades = false,
    extra_newline = false,
    font = 'Hack:size=10',
    gap_x = 20,
    gap_y = 20,
    minimum_width = 254,
    minimum_height = 250,
    maximum_width = 254,
    net_avg_samples = 2,
    no_buffers = true,
    out_to_console = false,
    out_to_ncurses = false,
    out_to_stderr = false,
    out_to_x = true,
    own_window = true,
    own_window_class = 'Conky',
    own_window_type = 'desktop',
    show_graph_range = false,
    show_graph_scale = false,
    stippled_borders = 0,
    update_interval = 1.0,
    total_run_times = 0,
    uppercase = false,
    use_spacer = 'none',
    use_xft = true,
}

conky.text = [[
${color grey}Info:$color ${scroll 26 lordpaijo - $sysname $nodename $kernel $machine}
$hr
${color green}Uptime:$color $uptime
${color cyan}Frequency (in MHz):$color $freq
${color cyan}Frequency (in GHz):$color $freq_g
${color red}RAM Usage:$color $mem/$memmax - $memperc% ${membar 4}
${color red}Swap Usage:$color $swap/$swapmax - $swapperc% ${swapbar 4}
${color red}CPU Usage:$color $cpu% ${cpubar 4}
${color orange}Processes:$color $processes  ${color grey}Running:$color $running_processes
$hr
${color white}File systems:
 / ${color lightgrey}${fs_used /}/${fs_size /} ${fs_bar 6 /}
${color white}Networking:
${color green}Up:$color ${color lightgrey}${upspeed} ${color white}- ${color red}Down:$color ${color lightgrey}${downspeed}
$hr
${color grey}Name              PID     CPU%   MEM%
${color lightgrey} ${top name 1} ${top pid 1} ${top cpu 1} ${top mem 1}
${color lightgrey} ${top name 2} ${top pid 2} ${top cpu 2} ${top mem 2}
${color lightgrey} ${top name 3} ${top pid 3} ${top cpu 3} ${top mem 3}
${color lightgrey} ${top name 4} ${top pid 4} ${top cpu 4} ${top mem 4}
]]
